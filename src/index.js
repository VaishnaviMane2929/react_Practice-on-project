import React from "react";
import ReactDom from 

const root  = ReactDom.creatRoot(document.getElementById 
    ('root'));
root.render (
    <React.StrictMode >
     <App />
    <React.StrictMode />

);
